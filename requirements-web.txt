# Minimal deps for running the Phase 1 web reporting app
# (no torch / OCR — keeps Docker image small)
requests>=2.31
tldextract>=5.0
flask>=3.1
gunicorn>=22.0
beautifulsoup4>=4.12
apscheduler>=3.10
playwright>=1.44
meta-ads-collector>=1.3.0
