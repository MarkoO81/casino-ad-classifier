services:
  classifier:
    build: .
    image: ghcr.io/markoo81/casino-ad-classifier:latest
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
    volumes:
      - ./config:/app/config
