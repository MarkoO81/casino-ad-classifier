# --- Core (lightweight, always required) ---
requests>=2.31
tldextract>=5.0

# --- Visual classifier (CLIP zero-shot + embeddings) ---
# torch is large; install the right wheel for your CUDA version from
# https://pytorch.org/get-started/locally/ — these specs are CPU-fallback.
torch>=2.1
open_clip_torch>=2.24
Pillow>=10.0

# --- OCR (in-image text extraction) ---
# Pick ONE of:
paddleocr>=2.7        # better accuracy on stylized banners
# easyocr>=1.7        # simpler install alternative

# --- Training a fine-tuned head (optional) ---
scikit-learn>=1.3     # for AUC / AP metrics

# --- Headless browser for ad_snapshot_url + JS redirects ---
# playwright>=1.40    # uncomment when you build the snapshot renderer
